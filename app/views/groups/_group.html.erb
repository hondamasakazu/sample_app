<li>
  <%= link_to group.name, group %>
  <% if current_user.admin? %>
    | <%= link_to "削除", group, method: :delete,
                                  data: { confirm: "削除しますか?" } %>
  <% end %>
</li>