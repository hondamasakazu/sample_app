<% provide(:title, "Edit Group") %>
<h1>Update group profile</h1>

<div class="row">
  <div class="span6 offset3">

    <%= form_for(@group) do |f| %>
      <%= render 'fields', f: f %>
      <%= render 'shared/error_messages', object: f.object %>
      <%= f.submit "保存", class: "btn btn-small btn-primary" %>
    <% end %>

  </div>
</div>